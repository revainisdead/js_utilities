// Inspired by jQuery's each.
//
// Example:
//     $.each((key, value) => console.log(key, value))

// Note: Write this for learning. No reason to write it. Use underscore: _.each()
function each(callback) {

    
    callback(key, value)
}
